// This file is kept for compatibility but no longer uses Prisma or NextAuth
// Authentication is now handled directly via backend API

export const authOptions = {
  // Minimal config for compatibility
  pages: { signIn: '/login' }
}
